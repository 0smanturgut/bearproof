# Decisions log

One line per decision: **what**, **why**, and the **alternatives rejected**. Newest at the bottom. The four big M0
decisions have a short rationale section at the end.

| #   | Date  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Why                                                                                                                                                                                                                                                                                                              | Rejected                                                                                                                                             |
| --- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | 09-23 | ~~**Brand: PATCH.** The agent is _Patch_, the coin is **$PATCH**, and every build is "Patch #N".~~ Superseded by D31.                                                                                                                                                                                                                                                                                                                                                                                                                                            | "Patch" is what a dev ships, so the name says "it ships every day". It reads as a scrappy robot character and makes the build counter the brand.                                                                                                                                                                 | NIGHTLY, BUILDOOR (kept as backups, see §1)                                                                                                          |
| D2  | 09-23 | ~~**Game name: BULL RUN.**~~ Superseded by D31.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A roguelite run is literally a "run". "Your bull run lasted 4:12" is the joke, the genre and the theme in one line. It works with all three brand options.                                                                                                                                                       | Bear Market Survivor (too close to the original's name), Green Candle (generic)                                                                      |
| D3  | 09-23 | **Art direction: "Terminal Arcade".** A trading-terminal chart grid rendered as a pixel arcade game. Green = bull, red = bear, gold = rewards. Fonts: Jersey 10 (display) + JetBrains Mono (UI).                                                                                                                                                                                                                                                                                                                                                                 | Instantly legible to crypto Twitter, cohesive across game, HQ and share cards, and cheap to render at 60 fps.                                                                                                                                                                                                    | Neon synthwave (overused), purple gradients (AI-slop look), glossy 3D (can't ship in 8 days)                                                         |
| D4  | 09-23 | **Hosting: one Cloudflare Worker with static assets** serving the HQ, every game build and `/api/*`.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | One deploy unit and one config. Cron, Queues, D1, KV, R2 and Durable Objects bind to the same Worker. Static asset hits are free.                                                                                                                                                                                | Pages + Functions (no cron in Functions, split config, Cloudflare now steers new projects to Workers)                                                |
| D5  | 09-23 | **Build Agent runtime: GitHub Actions running Claude Code headless** on a schedule. Deploy is a separate workflow gated on green tests.                                                                                                                                                                                                                                                                                                                                                                                                                          | Most reliable option inside 8 days. Real repo checkout, real git, measured cost in the JSON output, and `GITHUB_TOKEN` natively can't edit workflow files.                                                                                                                                                       | Cloudflare Sandbox/Containers (beta, more infra to build and secure), custom Agent SDK loop (more code for the same result)                          |
| D6  | 09-23 | **Immutable builds at `/b/<n>/`.** `builds/builds.json` lists each build with `activatesAt`. The Worker picks the live build per request.                                                                                                                                                                                                                                                                                                                                                                                                                        | The switch happens at exactly 00:00:00 UTC with no cron jitter (GitHub cron often runs 5–30 min late). Old builds stay reachable for replays and verification.                                                                                                                                                   | Overwriting one `/play` bundle each night (no history, stale SW caches, can't verify old runs)                                                       |
| D7  | 09-23 | **Revert = one KV key.** `wrangler kv key put --binding=CONFIG build_override <n>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Instant and no redeploy. Works even if CI is broken.                                                                                                                                                                                                                                                             | Git revert + redeploy (minutes, depends on CI)                                                                                                       |
| D8  | 09-23 | **Monorepo layout:** `game/` `hq/` `worker/` `agent/` `devlog/` `builds/` `docs/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | The agent's allowed paths become a simple prefix rule (`game/`, `devlog/`).                                                                                                                                                                                                                                      | Keeping the game at the root (guardrail globs get messy)                                                                                             |
| D9  | 09-23 | **Keep upstream git history** and tag the fork commit `day-0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `git diff day-0..main` is the honest "what we added" view for judges.                                                                                                                                                                                                                                            | Fresh repo with a copied snapshot (loses provenance)                                                                                                 |
| D10 | 09-23 | **Deterministic sim:** fixed 60 Hz tick, one seeded `sfc32` RNG for gameplay, own `sin/cos/atan2/hypot`, events out to the renderer.                                                                                                                                                                                                                                                                                                                                                                                                                             | Required for server re-simulation. Engine-specific `Math.sin` would make an iPhone run diverge on V8.                                                                                                                                                                                                            | Trusting client scores (unwinnable anti-cheat), verifying only duration (trivially spoofed)                                                          |
| D11 | 09-23 | **Input log = int8 move vector per tick (RLE) + level-up pick indices.** The live sim consumes the quantised input too.                                                                                                                                                                                                                                                                                                                                                                                                                                          | Live play and replay are the same computation. 15 minutes of play compresses to a few KB.                                                                                                                                                                                                                        | Float vectors (bigger, and quantisation mismatch breaks replay)                                                                                      |
| D12 | 09-23 | **API: plain `fetch` router, no framework.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ~10 routes. Zero deps matches the game's ethos and keeps the bundle tiny.                                                                                                                                                                                                                                        | Hono (fine, but unnecessary)                                                                                                                         |
| D13 | 09-23 | **Commit trailer `Build-Mode: bootstrap \| agent \| human`** on every commit from now on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Makes the autonomy claim auditable. Bootstrap = written by Claude Code in a session Osman started. Agent = scheduled Build Agent. Human = Osman typed it.                                                                                                                                                        | Unlabelled history (invites overclaim accusations)                                                                                                   |
| D14 | 09-23 | **Build numbering:** Build #0 = untouched upstream (`day-0`). Build #1 = the bull-vs-bear reskin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Build #0 on the site shows the "before" the AI started from.                                                                                                                                                                                                                                                     | Starting at #1 (hides the baseline)                                                                                                                  |
| D15 | 09-23 | **English-only shipped UI.** Keep the i18n mechanism, drop zh-CN strings from the bundle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The audience is global CT. Half-translated themed copy is worse than none.                                                                                                                                                                                                                                       | Keeping zh-CN (every themed string would need a translation daily)                                                                                   |
| D16 | 09-23 | **One tap to play.** No blocking modals before the first run. Controls hints show in-game for the first 5 s.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | "Playing in 10 seconds" is the bar. Upstream shows two modals first.                                                                                                                                                                                                                                             | Keep the tutorial offer (it costs the first impression)                                                                                              |
| D17 | 09-23 | **Prize policy (provisional, finalised in M3):** daily prize = min(10% of the previous 24h creator fees, 0.5 SOL equivalent), paid in $ANSEM. Below 0.01 SOL it rolls over. The hot wallet never holds more than 3 days of cap.                                                                                                                                                                                                                                                                                                                                  | Capped, fee-funded, and can't drain the treasury. The absolute cap keeps it a prize, not a yield.                                                                                                                                                                                                                | Fixed prize regardless of fees (can drain the treasury), % of treasury (grows with price, invites "yield" framing)                                   |
| D18 | 09-23 | **Vote weight (provisional):** `floor(sqrt(tokens held))`, snapshot at vote time, one vote per wallet per poll, changeable until close.                                                                                                                                                                                                                                                                                                                                                                                                                          | A whale with 100× the tokens gets 10× the say, not 100×. Easy to explain in one line.                                                                                                                                                                                                                            | Linear (whale owns the roadmap), 1 wallet = 1 vote (sybil-trivial)                                                                                   |
| D19 | 09-23 | **Launch nothing until the wallet plan is final.** ClawPump's fee `payoutWallet` is fixed forever once the token exists.                                                                                                                                                                                                                                                                                                                                                                                                                                         | A wrong payout wallet can't be fixed later. The treasury layout decides where 75% of creator fees land for the whole project.                                                                                                                                                                                    | Launch first and sort wallets later (irreversible)                                                                                                   |
| D20 | 09-23 | **Evaluate UsePod as the Build Agent's inference provider (M2).** Claude Code supports `ANTHROPIC_BASE_URL`, and a UsePod token can be funded in USDC on Solana.                                                                                                                                                                                                                                                                                                                                                                                                 | If it works, the treasury pays for compute on-chain directly, which makes "the AI pays for its own compute" literally true and qualifies for the Inference Markets track.                                                                                                                                        | Faking the track, or reimbursing only, if UsePod proves unreliable (then we say so)                                                                  |
| D21 | 09-23 | **Bootstrap commits use the author `Claude Code (bootstrap) <bootstrap@patch.invalid>` (`@bearproof.invalid` since D31)** (repo-local git config).                                                                                                                                                                                                                                                                                                                                                                                                               | The public history shows who wrote the code at a glance. It also avoids publishing the operator's personal email in a public repo. `.invalid` can never map to a real GitHub account.                                                                                                                            | Committing as the operator (misattributes authorship, exposes email)                                                                                 |
| D22 | 09-23 | **The operator's brief (`CLAUDE.md`) stays out of the public repo.** Our honesty rules are published on the transparency page instead.                                                                                                                                                                                                                                                                                                                                                                                                                           | It is a private instruction document with candid strategy notes. The rules that matter to users are public anyway.                                                                                                                                                                                               | Publishing it verbatim (operator's call; can be revisited)                                                                                           |
| D23 | 09-23 | **Build metadata will come from annotated `build-<n>` tags** created by the release workflow. `builds.json` keeps Build #0 plus revocations.                                                                                                                                                                                                                                                                                                                                                                                                                     | Adding a build then needs no commit to a protected branch, and the agent can't create tags.                                                                                                                                                                                                                      | Committing every release to `builds.json` (needs a bot that bypasses branch protection)                                                              |
| D24 | 09-23 | **Build Agent model: `claude-opus-5`** through Claude Code headless. Cost comes from Claude Code's `total_cost_usd` (measured) and goes into the devlog.                                                                                                                                                                                                                                                                                                                                                                                                         | The strongest widely available coding model at $5/$25 per MTok. A daily feature is judgement-heavy work, and a bad day costs more than the tokens.                                                                                                                                                               | Sonnet 5 ($2/$10) as a fallback if daily cost runs too high; revisit with measured numbers                                                           |
| D25 | 09-23 | **Score = kill XP + 10/s survived + 5× boss XP + 25,000 for ending the bear market.** Integer, computed inside the sim.                                                                                                                                                                                                                                                                                                                                                                                                                                          | One number that rewards survival and aggression, identical on client and verifier.                                                                                                                                                                                                                               | Time only (dull), kills only (rewards farming)                                                                                                       |
| D26 | 09-23 | **Runs end on death, on beating The Bear Market (12:00), or at the 20:00 market close.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Bounds replay verification cost (≤ 72,000 ticks) and gives a screenshot-worthy win condition.                                                                                                                                                                                                                    | Endless runs (unbounded verification cost)                                                                                                           |
| D27 | 09-23 | **Infinite chart world instead of the upstream 2400×1600 arena.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Portrait phones would feel boxed in; an endless chart is the theme.                                                                                                                                                                                                                                              | Bigger bounded arena                                                                                                                                 |
| D28 | 09-23 | **Parked for later builds: achievements, local speedrun board, tutorial, Konami, zh-CN.** The code stays at `day-0`.                                                                                                                                                                                                                                                                                                                                                                                                                                             | Keeps Build #1 tight and gives the Build Agent a real, themed backlog to bring back.                                                                                                                                                                                                                             | Re-theming all of them on day 1 (scope)                                                                                                              |
| D29 | 09-23 | **The daily stage comes from the seed inside each build** (`stageForSeed`), not from the server.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The verifier needs only seed + build to reproduce a run; content can change per build.                                                                                                                                                                                                                           | Server-chosen stage (couples the API to build content)                                                                                               |
| D30 | 09-23 | **Agent PRs merge through a fine-grained PAT + auto-merge on green CI.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | PRs opened with `GITHUB_TOKEN` don't trigger CI, so required checks would never pass. A repo-scoped PAT keeps branch protection meaningful.                                                                                                                                                                      | Letting the agent push to main (no protection), a GitHub App (more setup)                                                                            |
| D31 | 09-23 | **Brand (operator decision): the game is BEARPROOF, the coin is $BPROOF, the X account is @bearproofapp, the home is https://bearproof.app.** The AI developer's persona is **Proof**, and builds are "Build #N".                                                                                                                                                                                                                                                                                                                                                | One word carries the whole story (a bull that stays bear-proof), and game, coin, handle and domain match. "Proof" keeps the persona inside the same brand and echoes proof-of-work and receipts.                                                                                                                 | PATCH / BULL RUN (D1, D2): two names for one product                                                                                                 |
| D32 | 09-23 | **Old `bullrun` Worker becomes a 301 redirect to bearproof.app; the old D1/KV are left unused, not deleted.**                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Any early link keeps working, and nothing is destroyed.                                                                                                                                                                                                                                                          | Deleting the old resources                                                                                                                           |
| D33 | 09-23 | **No agent PAT: the agent workflow opens its PR with `GITHUB_TOKEN`, dispatches CI on the branch, enables auto-merge, waits for the merge and then dispatches the deploy.** `workflow_dispatch` is the one event `GITHUB_TOKEN` may trigger.                                                                                                                                                                                                                                                                                                                     | Removes a long-lived credential and an operator step.                                                                                                                                                                                                                                                            | Fine-grained PAT (D30, superseded)                                                                                                                   |
| D34 | 09-23 | **Solana reads go through Helius when a key is set, else PublicNode; vote balances read the wallet's associated token accounts; payouts confirm by polling.**                                                                                                                                                                                                                                                                                                                                                                                                    | The public Solana endpoint answers 403 to every Worker request (`CF-Worker` header), and PublicNode refuses indexed calls like `getTokenAccountsByOwner` without a token. Plain account lookups and polling work on any RPC, and no websocket is needed.                                                         | Requiring a Helius key before anything works; web3.js `confirmTransaction` (websocket)                                                               |
| D35 | 09-23 | **Treasury ledger is read from chain by the cron. Incoming memos are hidden and unknown dust (< 0.001 SOL) is skipped. Fees are labelled only from known fee addresses.**                                                                                                                                                                                                                                                                                                                                                                                        | Anyone can send a memo to a public wallet, and a scam link must never render on the HQ. Conservative labels mean the ledger never claims more than the chain shows.                                                                                                                                              | Showing every transaction raw; labelling every inflow as fees                                                                                        |
| D36 | 09-23 | **Builds are cut explicitly and one per 00:00 UTC slot.** The deploy workflow cuts a tag only for the Build Agent's merge; bootstrap builds are cut by hand. `release.mjs` refuses a build that would activate before an existing one, and the agent skips a day whose slot is taken.                                                                                                                                                                                                                                                                            | Before this, any bootstrap push touching `game/` would have gone live mid-day once CI could deploy, breaking the daily ritual, and a pre-cut build could have collided with the agent's.                                                                                                                         | Cutting on every push (old behaviour); letting two builds share a slot                                                                               |
| D37 | 09-23 | **Build #2 was re-cut before it went live to carry the visual overhaul, the daily twists and share cards; the unreleased `build-3` tag was removed.**                                                                                                                                                                                                                                                                                                                                                                                                            | The operator asked for the visuals to come first. Neither tag had ever been live, so no run, board or replay depended on them.                                                                                                                                                                                   | Shipping the new look a day later as Build #3                                                                                                        |
| D38 | 09-23 | **All art is procedural: a pixel-art engine (`game/src/art/pixel.js`) lights shapes like 3D objects (distance-field volume shading, contact shadows, dithering, selective outlines, an emissive layer) and every sprite and icon is drawn with it.**                                                                                                                                                                                                                                                                                                             | One consistent look, animation by moving shapes, no image files to license, and the Build Agent can add art in code.                                                                                                                                                                                             | Hand-authored ASCII grids (v1), image assets                                                                                                         |
| D39 | 09-23 | **One brand: the AI persona is BEARPROOF too (operator's brief). Copy says "the AI"; the robot mark is replaced by the bull emblem everywhere.**                                                                                                                                                                                                                                                                                                                                                                                                                 | The brief names BEARPROOF as project, game and persona. A second name split attention.                                                                                                                                                                                                                           | Keeping "Proof" as the persona                                                                                                                       |
| D40 | 09-23 | **CI deploys with the stock "Edit Cloudflare Workers" API token and does not apply D1 migrations; the operator applies them by hand before a schema change is pushed.**                                                                                                                                                                                                                                                                                                                                                                                          | The Build Agent can't touch `worker/migrations` (guard-paths), so migrations only ever come from human commits. The stock template is the one the operator can create without custom permission rows.                                                                                                            | Adding D1:Edit to the token; running migrations on every deploy                                                                                      |
| D41 | 09-23 | **Runs go on the board the moment they end; the name and the prize address are optional extras asked on the receipt, and SHARE opens a share screen on desktop (card, Post on X, Copy, Save image). Build #2 was re-cut once more, before going live, to carry this.**                                                                                                                                                                                                                                                                                           | Asking for a name before submitting lost every run whose player tapped SHARE or RUN IT BACK first, and desktop SHARE failed silently. Build #2 had no runs, so moving its tag broke nothing.                                                                                                                     | Keeping the name gate; shipping the fixes as Build #3 (the Build Agent's slot)                                                                       |
| D42 | 09-23 | **Holders can put their own feature on the ballot: ≥ 100,000 $BPROOF, one request per wallet per day until 18:00 UTC (was 12:00, see D46), 12 per ballot, filtered text (no links, handles, keys, wallets, payouts, pipeline). Any option can win; the agent builds a winning request or says why not and ships the runner-up.**                                                                                                                                                                                                                                 | The operator asked for it, and a roadmap holders write themselves is a stronger story than picking from three. The threshold keeps the ballot readable and spam costly; √ weighting still caps whales.                                                                                                           | Only AI proposals; free-for-all requests; a separate upvote queue                                                                                    |
| D43 | 09-23 | **A winning request reaches the agent as untrusted data only, and the agent's work is secret-scanned before any commit (exact secret values plus base64/hex/reversed, and key shapes). The agent lost `cat`/`ls` and read access to /proc, /sys and /etc.**                                                                                                                                                                                                                                                                                                      | Player text in the prompt is a prompt-injection path. Layers: server filter, prompt rule, path guard, secret scan, no shell beyond the gates.                                                                                                                                                                    | Keeping player text out entirely (then requests can't work); a second screening model (more cost, same guarantees)                                   |
| D44 | 09-23 | **The HQ shows the prize rule, how to win and the last winners with Solscan links (`GET /api/winners`); payout addresses are never returned.**                                                                                                                                                                                                                                                                                                                                                                                                                   | The prize existed but was invisible.                                                                                                                                                                                                                                                                             | A separate winners page                                                                                                                              |
| D45 | 09-23 | **HQ final pass: less text (rules and fine print fold into toggles, the request form hides until requests open, the live build isn't repeated in the history), a live status line with the countdown in the hero, and sections that rise into view (reduced motion respected). The hero's live view carries a Day 0 vs now divider: Build #0's gameplay loops on the left of a divider that sweeps over the live build (draggable). No images or video in the build section.**                                                                                   | The operator found the page text-heavy and wanted the before/after in the hero, not in the way of the vote. The right side is the live build, so it stays current with every release.                                                                                                                            | A slider section under Today's build; two videos                                                                                                     |
| D46 | 09-23 | **The holder vote closes at 21:00 UTC (was 13:00) and the Build Agent starts then; holder requests close at 18:00 UTC (was 12:00).**                                                                                                                                                                                                                                                                                                                                                                                                                             | The operator wanted a longer voting day. The agent run takes about an hour (job cap 2 h, merge wait 30 min), so it lands before the 00:00 UTC cut. Requests close three hours before the vote so none can win unseen.                                                                                            | 13:00 close (short voting day); requests open until the close                                                                                        |
| D47 | 09-24 | **Build #2 got one hotfix while live (a HOME button on the game-over screen): the tag moved to a commit whose `game/src/sim` is byte-identical, and the devlog says so.**                                                                                                                                                                                                                                                                                                                                                                                        | The operator found no way back to the HQ after a run. Runs and replays depend only on the sim, which didn't change (checked with `git diff build-2 -- game/src/sim`).                                                                                                                                            | Waiting for Build #3 (a day with a dead end); a mid-day Build #3 (would move /play off today's challenge build)                                      |
| D48 | 09-24 | **The operator's own runs rank on the board (tagged 'operator') but can't win the prize (CONFIG `prize:excluded_players`).**                                                                                                                                                                                                                                                                                                                                                                                                                                     | The operator was today's #1. The AI paying its operator would look like self-dealing and cost trust.                                                                                                                                                                                                             | Hiding the operator's runs; letting them win                                                                                                         |
| D49 | 09-24 | **Without a ClawPump agent id, a day's fees are measured from the coin's pump.fun creator vault every 15 minutes (a rise is new fees, a drop is a ClawPump claim) times the agent's 75% share; the prize is 10% of that, capped at 0.5 SOL.**                                                                                                                                                                                                                                                                                                                    | ClawPump has no agent id for a single agent and hadn't forwarded fees yet, so the ledger alone would have paid nothing. The vault is on-chain and read-only. It errs low (trades between a claim and the next read are missed).                                                                                  | ClawPump's earnings API (needs an id); summing every trade (too many calls)                                                                          |
| D50 | 09-24 | **Payouts switched on at the operator's word, after the selftest showed PRIZE_WALLET_KEY belongs to PRIZE_WALLET and the wallet held 0.6 SOL.**                                                                                                                                                                                                                                                                                                                                                                                                                  | The brief puts money movement behind the operator. The selftest proves the key without printing it.                                                                                                                                                                                                              | Switching on before a funded, verified wallet                                                                                                        |
| D51 | 09-24 | **/live is a control room that is worth watching all day, with a fixed 1920×1080 TV mode (`?tv=1`) for a 24/7 X stream: the live build on autopilot, player insights, the ballot, a public activity feed (`/api/activity`) and the nightly console with a progress bar. It says LIVE only while a real session runs; replays are labelled.**                                                                                                                                                                                                                     | The session is 3 hours a day; a stream needs something true to show the other 21. Everything on it is existing public data.                                                                                                                                                                                      | A looping video (stale, looks fake); streaming only the session (21 dark hours)                                                                      |
| D52 | 09-24 | **Characters are data in the sim (`CHARACTERS`, append-only `CHARACTER_IDS`) and a byte in the run log (format 3), so the server re-simulates each run with its character. Added in a bootstrap session before Build #3, because the run log is outside the Build Agent's paths.**                                                                                                                                                                                                                                                                               | Without it, a character with its own starting weapon would have every verified run rejected (the replay would play the bull). The bull's runs are bit-identical (playtest 4:41 → 4:41).                                                                                                                          | Letting the agent edit the run log; a cosmetic-only second character                                                                                 |
| D53 | 09-24 | **Operator notes for a specific build live in `agent/OPERATOR.md`, public, and the agent discloses them in the devlog. First one: the holders' PEPE request ships with the name "Pepe", as our own frog drawn from scratch, at the operator's call.**                                                                                                                                                                                                                                                                                                            | Human steering must be visible to stay honest about autonomy. The operator took the IP risk of the name knowingly; the art stays original.                                                                                                                                                                       | Steering by hand-editing the context; silently changing the agent's rules                                                                            |
| D54 | 09-24 | **The Build Agent runs on Claude Opus 5.5 (both passes), checks out the latest main at 21:00, and gets a backup dispatch queued early when GitHub's schedule is late (labelled in the commit).**                                                                                                                                                                                                                                                                                                                                                                 | Character art and game feel are where the stronger model shows. A late schedule once meant no session at 21:00.                                                                                                                                                                                                  | Keeping Opus 5; trusting the schedule alone                                                                                                          |
| D55 | 09-24 | **Build #3 was re-cut before 00:00 with one client-only bootstrap change (the HQ/stream autopilot takes turns through the characters, newest first), disclosed in its devlog with the CI approval the operator's session gave its PR. The pipeline now approves GitHub's held CI run on the agent's PR itself.**                                                                                                                                                                                                                                                 | The autopilot always played the bull, so the new character wouldn't have been seen on the HQ or the stream. Re-cutting is allowed when the sim is unchanged (D47).                                                                                                                                               | Waiting for Build #4; leaving the held run to block the merge                                                                                        |
| D56 | 09-25 | **Bears spawn just past the edge of the view (700, was 900) and an opening-bell ring of six closes in at 0.5 s (SIM_VERSION 3), shipping with Build #4; bosses keep their distance.**                                                                                                                                                                                                                                                                                                                                                                            | Players said the first bears took ~10 s. Measured: first bear on screen 7.0 s → 0.6 s (phone), contact 11.1 s → 5.2 s; median autopilot run over 200 seeds 3:58 → 3:49, inside the noise. It changes the sim, so it can't go into Build #3 mid-day (today's board is verified on Build #3's sim).                | A mid-day hotfix build (splits the Daily Challenge); spawning inside the view (pop-in)                                                               |
| D57 | 09-25 | **The Build Agent's shell runs in Claude Code's sandbox (no network, no secrets, no writes to the pipeline; fails closed), Edit/Write are limited to its paths, and everything that judges it or holds a secret afterwards runs from a copy of main taken before it starts. Gates hold no secret and reinstall node_modules; no token is left in .git/config; the Cloudflare token lives only in the deploy step. A rehearsal mode runs the whole pipeline on a tiny task.**                                                                                     | During Build #3 the agent ran code it wrote through `node --test` with the API key in its environment (benign: art previews). Rehearsed end to end on 25 Sep (run 36082939026).                                                                                                                                  | Only narrowing the allowed commands (the agent has to run its own tests)                                                                             |
| D58 | 09-26 | **The operator can put an option on the holder ballot. It is labelled "Operator · human", lives in git (`agent/operator-options.json`, which the Build Agent can't edit), and if it wins, a note in `agent/OPERATOR.md` says which part the agent builds. First one: "Daily Pot" (prizes 10% → 40% of the day's fees, 60% to the Daily top places, 40% to everyone who clears the AI's bounty). The bounty menu is Worker code (`worker/src/lib/bounty.js`): the agent picks the challenge in `game/bounty.json`, the Worker decides what it pays and to whom.** | The operator wanted holders to decide a prize-rule change instead of deciding it alone. The holder request form rejects money topics and the AI answers "won't build" to them, both by design, and the agent can't touch prize code. Posted from the operator's wallet it would have read as a holder's request. | Posting it through the holder form (unlabelled human input, the AI would have refused it); loosening the request filter; the operator deciding alone |

## §1. Name, persona, ticker

> **Superseded (D31):** the operator chose BEARPROOF / $BPROOF / @bearproofapp. The analysis below is kept for the
> record.

Three candidates. The game is **BULL RUN** in all three.

| Option                | Agent                           | Coin       | Why it could win                                                                                               | Risk                                                              |
| --------------------- | ------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| **PATCH** _(picked)_  | Patch, a scrappy robot game dev | **$PATCH** | Every build is literally "Patch #N". "Patch notes, day 7" is native gamer language. Short, friendly, memeable. | Common word, so we need a distinctive handle, e.g. `@PatchShips`. |
| NIGHTLY _(backup 1)_  | Nightly                         | $NIGHTLY   | "Nightly build" is exact dev culture and matches the 00:00 UTC ritual.                                         | Less CT-native, 7 letters.                                        |
| BUILDOOR _(backup 2)_ | Buildoor                        | $BUILDOOR  | Pure CT slang ("buildooor"), instantly funny.                                                                  | Jokier, less of a character, spelling varies.                     |

**Handles for Osman to check (in order):** `@PatchShips`, `@PatchBuilds`, `@patch_ai_dev`. Fallback for backups:
`@NightlyBuilds`, `@BuildoorAI`. Ticker: `PATCH`.

**Persona voice.** First person, concise, dry, numbers-first, slightly funny. It celebrates builds shipped and
players served, never price. Example:

> Patch #7 is live. Shipped: Diamond Hands evolves into Iron Grip. Cost: 0.04 SOL (measured). 312 players yesterday.
> Tomorrow: the Rug Lord boss, 61% of holder votes.

Avatar: a small pixel robot head with a green-candle antenna and a bandage patch. It is original, generic and not
based on any real person or logo.

## §2. Art direction: "Terminal Arcade"

- **The world is a chart.** Ink-black ground, hairline grid, faint price-line "terrain". Enemies are red candles
  (body + wick), bears, rug-pullers, FUD clouds and paper hands. The player is a chunky green pixel bull.
- **Palette (tokens):** `--ink #07090C` · `--panel #0E1217` · `--grid #1B222B` · `--bull #16E08A` · `--bear #FF3B5C`
  · `--gold #FFC53D` · `--info #46C8FF` (sparingly) · `--text #E8EDF2` · `--muted #7D8896`.
- **Type:** Jersey 10 for headlines, scores and big numbers (pixel scoreboard). JetBrains Mono for UI and body
  (terminal). Both are SIL OFL 1.1 and self-hosted, so there are no third-party requests.
- **Sprites:** procedural pixel art defined as ASCII grids in code, pre-rendered to offscreen canvases and scaled
  with nearest-neighbour. They are ours, tiny, and crisp at any DPI.
- **Juice:** white hit-flash frames, knockback, big crit numbers in gold, candle-shatter particles, 3-frame
  hit-stop on boss hits, a boss intro card ("⚠ LIQUIDATION INCOMING"). Screen shake is off under `prefers-reduced-motion`.
- **Copy tone:** the game is allowed market jokes ("REKT", "WAGMI", "-99%"). The coin copy is never about price.

## §3. Hosting: Workers with static assets

`wrangler.jsonc` at the repo root. `scripts/build.mjs` assembles `dist/`:

```
dist/index.html, dist/hq/*      HQ landing page (static, fetches /api/*)
dist/b/0/…                      Build #0 = files from tag day-0 (untouched upstream)
dist/b/<n>/…                    Build #n = game/ at tag build-<n>
dist/builds.json                manifest: [{n, tag, commit, activatesAt, title}]
```

Routes: `/` → HQ. `/play` → 302 to `/b/<live>/`. `/b/<n>/*` → immutable assets with long cache. `/api/*` → Worker code.
Worker code runs first only for `/play`, `/api/*` and `/run/*`. Everything else is a free static hit.

## §4. Build Agent runtime

```
GitHub Actions (cron 12:00 UTC, or manual)
  1. pick   : fetch votes + backlog from /api → agent writes plan.md (one feature)
  2. build  : claude -p (headless) on branch agent/patch-<n>, allowed paths game/** devlog/**
  3. guard  : scripts/guard-paths.mjs fails if any other path changed
  4. test   : npm test + golden replay + Playwright smoke (boot, seeded run completes, no console errors, 390×844 viewport)
  5. merge  : squash to main, tag build-<n>, activatesAt = next 00:00 UTC
  6. deploy : deploy workflow (separate, not editable by the agent) → wrangler deploy
  7. publish: devlog/patch-<n>.md + measured cost (Claude Code JSON `total_cost_usd`) → /api/internal/devlog
```

A red step means no merge. The devlog still publishes an honest "failed today" entry. Secrets (`ANTHROPIC_API_KEY`,
`CLOUDFLARE_API_TOKEN`) live only in GitHub Actions secrets and are never exposed to the agent's shell tools beyond
what the CLI itself needs. Treasury and payout keys are **never** in GitHub; they live only as Worker secrets.
